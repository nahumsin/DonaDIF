<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nahumsin.donadif">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <activity
        android:name="com.facebook.FacebookActivity"
        android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Translucent.NoTitleBar" />

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".CrearCuenta"
            android:label="@string/create_interface"
            android:parentActivityName=".login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ConfirmarDonativo"
            android:label="@string/title_activity_confirmar_donativo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".CanastasBasicas"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".seleccionarFamilia"
            android:parentActivityName=".CanastasBasicas" />
        <activity android:name=".ActivityAnadirFamilia" />
        <activity android:name=".ActivityListaFamilia_Donador" />
        <activity android:name=".Activity_ModificarFamilia" />
        <activity
            android:name=".ListadoModFamilia"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity_Admin" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

    </application>

</manifest>